import { styled } from "styled-components";

const StyledBoxTemplate = styled.div`
  width: 100%;
  border-radius: var(--border-radius-lg);
  border: 1px solid var(--color-zinc-100);

  h1 {
    text-align: center;
    padding: 1rem 2rem;
  }
`;

export default StyledBoxTemplate;
